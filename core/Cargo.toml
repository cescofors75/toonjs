[package]
name = "toon-core"
version = "0.1.0"
edition = "2021"
description = "ToonJS columnar engine (Rust -> WASM core)"
license = "MIT"

[lib]
crate-type = ["cdylib"]
path = "src/lib.rs"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
